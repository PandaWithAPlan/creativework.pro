# Creative Hub

**Creative Hub** — это современный статический веб-сайт для цифрового агентства и платформы профессионального сообщества. Проект разработан с использованием чистого HTML, Tailwind CSS и Vanilla JavaScript, обеспечивая высокую производительность и простоту поддержки.

## 📋 Описание проекта

Проект представляет собой публичную точку входа в экосистему **Creative Work Pro Hub**. Архитектура сайта отражает трехуровневую модель бизнеса:
1. **Community Hub** (Ядро)
2. **Services & Tools** (Инфраструктура)
3. **Innovation Lab** (Витрина R&D)

Дизайн реализован в стиле "Split Screen" (разделенный экран) для десктопов, с адаптацией под мобильные устройства (Mobile First).

## 🛠 Технологический стек

*   **Frontend:**
    *   HTML5 (Семантическая верстка)
    *   [Tailwind CSS](https://tailwindcss.com/) (Стилизация)
    *   JavaScript (ES6+) (Логика взаимодействия, формы, анимации)
    *   CSS3 (Кастомные анимации и скроллбары)
*   **Инструменты разработки:**
    *   Node.js & npm (Управление зависимостями)
    *   Python (Автоматизированная верификация и тестирование)
    *   Playwright (E2E тестирование/скриншоты)

## 📂 Структура проекта

```text
├── css/                 # Глобальные стили и анимации
│   └── animations.css   # Keyframe анимации
├── js/                  # JavaScript логика
│   ├── forms.js         # Обработка и валидация форм
│   ├── scroll-animation.js # Анимации при скролле
│   ├── analytics.js     # Заглушка для аналитики
│   └── main.js          # Основные скрипты
├── verification/        # Скрипты для верификации верстки
│   ├── verify_all.py    # Основной скрипт проверки (Python + Playwright)
│   └── *.png            # Скриншоты-результаты проверок
├── *.html               # Основные страницы сайта (index, about, services, etc.)
├── package.json         # NPM зависимости
└── README.md            # Документация проекта
```

## 🚀 Установка и запуск

Поскольку проект является статическим, для его работы не требуется сложная настройка серверного окружения.

### Предварительные требования
*   Браузер (Chrome, Firefox, Safari, Edge)
*   Python 3.x (для запуска верификации)
*   Node.js (опционально, для управления пакетами)

### Запуск сайта
1.  Клонируйте репозиторий:
    ```bash
    git clone <repository-url>
    ```
2.  Откройте файл `index.html` в любом современном браузере.

### Установка зависимостей (для разработки)
```bash
npm install
```

## 🧪 Тестирование и Верификация

В проекте настроена автоматическая система верификации внешнего вида страниц с использованием Python и Playwright.

### Настройка окружения Python

1.  Установите Playwright для Python:
    ```bash
    pip install playwright
    playwright install
    ```

### Запуск проверок
Скрипт `verify_all.py` открывает основные страницы сайта, делает скриншоты и сохраняет их в папку `verification/` для визуального контроля.

```bash
python3 verification/verify_all.py
```

## ✨ Функциональность

*   **Адаптивный дизайн:** Split-screen на десктопе, единая колонка на мобильных устройствах.
*   **Формы:** Валидация Email и обязательных полей на клиенте (Newsletter, Contact Form).
*   **Анимации:** Плавное появление элементов при скролле (`fade-in-section`).
*   **Навигация:** Интерактивное меню с подсветкой активных разделов.

## 📄 Лицензия

Creative Hub © 2023. Все права защищены.
